# Reorder README: LLM section before webcam section

Completed: 2026-09-05 12:37:36 PM EDT

## Prompt

Move the local LLM chat/benchmark section (and its performance metrics) to the start of
the README, before the webcam gesture quadcopter section.

## What changed

- `README.md`: swapped the order of the two `#` workstream sections so "Local LLM chat +
  benchmark" (setup, commands, performance table) now comes first, "Webcam gesture
  quadcopter control" second. Updated the intro bullet list to match the new order.

## Verification

- Read the full file after the edit: heading order is now `# Local LLM chat + benchmark`
  → `# Webcam gesture quadcopter control`; anchor links in the intro list (`#local-llm-chat--benchmark`,
  `#webcam-gesture-quadcopter-control`) still match their target headings' generated
  anchors regardless of position.

## Notes, assumptions, limitations

- This is on the `requirements-cleanup` branch, which was rebased onto latest `main`
  earlier in this session and already pushed once with its pre-rebase history — pushing
  this (and the rebase) requires `--force-with-lease`, not done automatically per the
  git-rebase skill's own instruction; still pending the user's go-ahead.
