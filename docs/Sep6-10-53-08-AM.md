# Push relocation and fix fist recognition

Completed 2026-09-06T10:53:08.991574-04:00 (America/New_York).

Explicit push authorization received. Verified origin is https://github.com/yav-fi/dnhacks26 and pushed relocation commit 54d31cc to ios-app-updates.

Investigated fist recognition. The previous remap required the thumb to satisfy a generic curled-joint heuristic and removed the model-fist fallback. This discarded confident Closed_Fist output when thumb geometry looked straight/uncertain. Added a regression which failed before the fix. Restored confident model fist acceptance when at least three other fingers are curled and none clearly extended. Thumb-up/down labels and clearly open fingers do not enter the fallback. Temporal hold filtering remains. Added command-candidate debug readout and removed the misleading thumb-tucked requirement in the guide.

Verification: Swift gesture regressions passed after reproducing failure before the fix; 29 phone-control tests passed; signed iOS build succeeded; whitespace check passed. Live receiver was unavailable during investigation; no live camera reproduction claimed. User’s iPhone 15 was unavailable, so fixed app has not been installed on it. Existing unrelated Podfile.lock change excluded.

Follow-up: installed and launched the corrected build on Ian’s iPhone 17 Pro at 10:53 AM America/New_York. Automatic approval review rejected pushing the new fix because it considered prior authorization limited to the relocation commit. Saving the fix locally only pending explicit approval.
