# Visible live drone and route annotations

Completed: 2026-09-05 13:09:08 EDT (America/New_York).

Request: ensure the prism appears at the path's live leading endpoint before release, leave spheres at each release, and show arrows moving forward along the route.

Replaced per-frame static position/orientation properties with dynamic callbacks so Cesium updates moving box geometry synchronously. The live box is opaque cyan. Added numbered orange release spheres with six-meter radii, one per manual-to-released transition. Added white arrow segments flowing forward along cumulative route distance using real time. Arrow count is bounded to 128; all markers and arrows clear on Reset. Route and original ghost persist between takeovers.

Verification: production build and TypeScript passed; diff whitespace check passed. Controller checks passed for stable dynamic position, exact live endpoint, unique release markers, forward arrow orientation and Reset cleanup. Browser screenshots confirmed the solid prism while piloting and sphere after release. Starting coordinate overlaps the Monument and can partially obscure these objects; streamed building collision avoidance is not implemented. Arrow animation was checked through the route implementation, not visually over a long flight.
