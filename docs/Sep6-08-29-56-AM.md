Completed: 2026-09-06T08:29:56.6265265-04:00 (America/New_York)

# Explain hand gesture rules

Reviewed freshly fetched origin/main at 60f2ba7 without changing the checkout to that revision.

Request: Read the latest repository and explain why hand poses were hardcoded instead of reusing earlier MediaPipe infrastructure.

Findings: iOS still runs MediaPipe GestureRecognizer and consumes its 3D world landmarks. Explicit finger bend/reach rules implement the requested fist, one-finger and two-finger vocabulary, with temporal voting. Prior notes record missed fist reports and camera orientation correction, but no measured proof that rules outperform the pretrained classifier. Earlier Python infrastructure also combined MediaPipe with geometric gestures; its optional local custom weights were not recovered in the historical search documented in the repo. Browser webcam recognition still uses canned MediaPipe gestures plus geometry.

Verification: fetched origin; read current remote Swift, browser and Python implementation plus historical prompt notes and gesture commit history. No runtime or physical-camera tests performed. No application code changed. Local branch was behind origin/main; no merge or checkout update performed.
