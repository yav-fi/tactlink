# Open palm = halt (stop in place)

Completed: 2026-09-05 08:09:42 PM EDT (America/New_York)

## Prompt

Add open palm -> "stop in place".

## What changed

- New action **`halt`** in `src/controls.py` `_start_maneuver`: clears the active
  maneuver, drops the leash, and pins `_alt_target` to the current altitude, so
  the drone freezes exactly where it is (cancels orbit / a dash / a climb /
  return-home mid-flight). Requires armed.
- `config/gesture_actions.json` - `"Open_Palm": "halt"` (was `null`); `__doc__`
  action list updated.
- `src/gestures.py` - `_DEFAULT_ACTION["Open_Palm"] = "halt"`; docstring.
- `src/controls.py` docstring; README gesture table.
- `src/main.py` demo - open palm at t=10 halts the orbit; timeline shifted so the
  gesture windows don't overlap.
- Tests: `test_fly_maneuver` +1 (`test_halt_cancels_a_maneuver_and_holds_position`),
  `test_sequences` `test_shipped_action_config` updated (`Open_Palm` -> `halt`).

## Verification

- `python -m py_compile src/*.py tests/*.py` clean; all suites green
  (operators 6, finger_swing 9, fly_maneuver 15, swarm 5, sequences 10).
- `--demo --check-gestures` log: `orbit 7.4s -> halt 10.4s -> fly_east 13.9s ...`.
- `--demo` render at t=12 s: `MODE HOLD`, `spd 0.1 m/s` - swarm frozen where the
  orbit was cancelled, not drifting.

## Notes

- Open palm was previously disabled for false positives; re-enabled because
  `halt` is a benign action (a spurious halt just hovers) and the finger/score
  filtering has since improved. Closed fist and Victory stay disabled.
