# Streaming, tok/s, and interactive REPL

Completed: 2026-09-05 12:04:17 PM EDT

## Prompt

Make `scripts/ask.sh` actually stream tokens (it was collecting the full reply before
printing anything), make the interaction more ollama-`run`-like (a real multi-turn chat
loop, not just single-shot), and show a tok/s readout per response.

## What changed

- `chat_client.py`:
  - `complete()` now returns the final `timings` dict via the generator's return value
    (`StopIteration.value`), so callers driving it manually can get both the streamed
    text and the throughput stats without a second request.
  - Added `Conversation`: a small multi-turn session object that keeps message history
    and re-renders the full chat template (via a new `_render_messages()`, generalized
    from the single-turn `_render_chat()`) on each `send()`, the way an ollama REPL
    keeps context across turns.
  - Added `_print_stream()` (prints chunks to stdout as they arrive, then a
    `[N tok in Ss -> X tok/s]` line to stderr) and a `_main()` CLI entry point:
    `python3 chat_client.py "question"` streams one answer with tok/s; no args drops
    into an interactive `>>>` REPL (exit with `/bye` or Ctrl-D).
- `scripts/ask.sh`: now execs straight into `chat_client.py`'s CLI instead of a
  one-off `chat_sync` call, so both one-shot and REPL modes work through the same
  entry point. No-args is now valid (starts the REPL) instead of a usage error.

## Verification

- Ran `./scripts/ask.sh "what's 17 * 23?"`: got a real token-by-token stream ending
  with `[95 tok in 1.05s -> 90.8 tok/s]`.
- Ran the REPL via piped stdin with a follow-up question referencing an earlier
  message ("my favorite color is teal" then "what is my favorite color?"); the second
  answer correctly recalled "teal", confirming multi-turn history is actually included
  in the rendered prompt (also checked directly by rendering `_render_messages` on a
  synthetic 3-message history and inspecting the `<|im_start|>...` turns).
- A meta self-reference question ("what did I just ask you?") got a confusing answer
  in one run — verified via the same direct-render check that the correct prior turn
  *was* present in the prompt, so this is the small quantized model's own behavior on
  a genuinely tricky self-referential question, not a history-tracking bug.

## Notes, assumptions, limitations

- tok/s shown per-turn is a real measurement from that turn's own `timings`, but a
  single short turn is not a rigorous benchmark — treat it as a live indicator, not a
  substitute for the still-pending gated 3-leg benchmark comparison.
- The REPL has no `/system` command or persistence across process restarts — each
  `ask.sh` invocation with no args starts a fresh `Conversation` with empty history.
