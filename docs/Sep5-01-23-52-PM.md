# Merge planner and mission integrations safely

- Completed: September 5, 2026 at 1:23:52 PM EDT (UTC-04:00)
- Prompt: Merge `yavin/drones2` into the latest `main`, preserve Ian's flight-command vocabulary, make planner failures HOLD, and prevent runtime-connected gesture mode from presenting the local quad simulator as authoritative.

## What changed

- Fast-forwarded `main` to `origin/main` and merged `yavin/drones2` without conflicts.
- Preserved the flight-language schema, documentation, tests, planner/runtime integration, and canonical `MissionCommand` input bridges.
- Changed planner-configured drones to HOLD when planning raises instead of falling back to legacy direct flight. Non-planner drones retain the direct path.
- Added a focused regression test for planner-exception HOLD behavior.
- Changed gesture runtime mode to skip construction and stepping of `QuadSimulator` and show a runtime status panel with the recognized gesture and mission submission result. Standalone mode retains the simulator view and behavior.

## Verification

- `.venv/bin/python -m pytest`: 39 passed, with two dependency deprecation warnings.
- Focused planner, integration, and flight-language tests: 18 passed.
- `.venv/bin/python -m py_compile src/main.py simulation/drone.py tests/test_simulation.py`: passed.
- Standalone and runtime-connected headless UI smoke runs were attempted but could not start because `cv2` is not installed in the existing `.venv`; `requirements.txt` declares `opencv-python>=4.8`.

## Notes

- No merge conflicts occurred.
- Thomas's `thomas/3d-mapping` branch and all `3d-mapping/` code were left untouched.
- Ian's flight-language schema remains separate from `simulation.models.MissionCommand`, as requested.
