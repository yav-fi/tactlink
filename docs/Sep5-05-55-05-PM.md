# Orbit faces the origin

Completed: 2026-09-05 05:55:05 PM EDT (America/New_York)

## Prompt

During orbit, point the drone's forward direction inward toward the origin
(instead of along the direction of travel).

## What changed

- `src/controls.py` orbit maneuver - `desired_yaw` now targets the inward radial
  (`-radial`) instead of the tangential velocity. The nose stays pointed at the
  origin while the drone circles (it crabs sideways). Yaw P-gain bumped 1.2 -> 1.5
  so it tracks the continuously-rotating target.
- `tests/test_fly_maneuver.py` - `test_orbit_converges...` now also asserts the
  forward vector `(-sin yaw, cos yaw)` aligns with `-pos` (dot > 0.9).
- README - orbit row notes "nose kept pointed inward".

## Verification

- `python -m py_compile` clean; `tests/test_fly_maneuver.py` 8/8,
  `tests/test_sequences.py` 10/10, `tests/test_finger_swing.py` 5/5.
- Rendered `--demo` mid-orbit: nose points at the origin, roll axis carries the
  tangential motion (ROLL bar active, PTCH ~0).
