# Clear stale runtime port

**Completed:** September 5, 2026 at 7:36:44 PM EDT (UTC-04:00)

## Prompt

Fix Uvicorn failing to start because port 8000 was already in use.

## Summary

Identified and stopped an orphaned instance of this repository's Uvicorn server that was still listening on `127.0.0.1:8000`.

## Changes

- Stopped stale process PID `30933`, whose command was `python -m uvicorn server.main:app --port 8000` and whose parent was PID 1.
- Added this prompt completion receipt; no application code was changed.

## Verification

- Confirmed the stale process was the sole listener on TCP port 8000 before stopping it.
- Confirmed port 8000 was free afterward.
- Successfully started the requested command, `.venv/bin/python -m uvicorn server.main:app --reload`.
- Observed Uvicorn bind to `http://127.0.0.1:8000` and complete application startup.
- Stopped the temporary verification server and confirmed port 8000 remained free for the user.

## Notes

If this recurs, `lsof -nP -iTCP:8000 -sTCP:LISTEN` identifies the listener. Inspect its command before using `kill <PID>` so an unrelated service is not stopped.
