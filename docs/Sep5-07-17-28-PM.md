# Add the iOS app on alvan-ios

Completed: 2026-09-05T19:17:28.958759-04:00 (EDT)

Request: Push the current iOS app to the `alvan-ios` branch of `yav-fi/dnhacks26`.

Added the standalone SwiftUI Xcode project under `ios/`, based on the repository's current main. Includes five-phone coordination, three-phone flat testing, radio-only relative maps, per-attempt distance statistics, logs, deployment scripts, and existing regression checks. This snapshot restores the working four-reading completion rule with no minimum sample span or deviation threshold, plus the original 250 ms peer completion grace and 300 ms coordinator handoff pause. The experimental first-reading handshake is reverted.

Preserved the existing workstreams and linked the iOS README from the root README. Excluded generated builds, provisioning profiles, device logs, caches, user-specific Xcode state, and the active room code.

Verification: inspected the staged changes and ran git diff --check. No broad tests were rerun for this repository import. The previously signed four-reading build 1788648913 was restored and launched on all three physical phones earlier in this session. The pre-rollback first-reading experiment is not the version being shipped here.

To build and deploy from this repository, run `cd ios` and follow its README. Xcode signing must use a development team and registered devices available to the developer. This adds the standalone app; it does not yet integrate its positions into the Python mission runtime.
