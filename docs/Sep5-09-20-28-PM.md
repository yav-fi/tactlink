# Merge `alvan-ios` into `main`

Completed: September 5, 2026 at 9:20:28 PM EDT (UTC-04:00)

## Prompt

Merge the iOS work from `alvan/ios` into `main` and report any discrepancies that may need manual resolution.

## What changed

- Fetched the latest remote refs and identified the requested branch as `origin/alvan-ios`.
- Merged `origin/alvan-ios` into `main` with merge commit `9c7d0a2` (`merge alvan ios`).
- Added the native Signal Map iOS app, tests, deployment tooling, documentation, and the root README workstream link from that branch.
- Preserved all pre-existing and concurrently edited `3d-mapping/`, root README, and launcher work without staging it.

## Verification

- Confirmed `origin/alvan-ios` is an ancestor of the merged `main` history.
- `git diff --check` completed without whitespace errors.
- Room tests: 73 checks passed.
- Deployment tests: 6 tests passed.
- Protocol integration: passed all 8 scenarios when run with local networking permission; the sandboxed run could not bind Network.framework listeners.
- Unsigned iPhoneOS build succeeded with Xcode 26.3 and `CODE_SIGNING_ALLOWED=NO`.

## Notes and discrepancies

- The remote branch uses the name `alvan-ios`, not `alvan/ios`.
- `ios/README.md` hard-codes `/Applications/Xcode 26.3.app/Contents/Developer`, while this machine's Xcode 26.3 is installed at `/Applications/Xcode.app/Contents/Developer`. Tests and the build pass with the installed path; developers may need to adjust `DEVELOPER_DIR` locally.
- The merge itself was clean and required no manual conflict resolution.
- Physical-device UWB behavior and signed installation were not exercised.
