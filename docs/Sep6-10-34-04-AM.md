# Phone gestures, orbit and operator display scale

Completed 2026-09-06T10:34:04.357246-04:00 (America/New_York).

Implemented the requested phone demo mapping: thumbs up/down ascend/descend, index orbit, palm hover overhead, fist come to the caller and retain follow, two fingers go to nearest other operator. Thumb direction is classified before fist to avoid accidentally calling follow. Old one/two/three-finger labels are disabled. Updated the existing styled guidance.

Nearest destination is measured from the caller, excludes the caller, locks per hold and stops on missing/stale destination data. Caller retains control; release stops motion. Existing hold timing and conflicts retained.

Orbit radius increased from 2 to 5 simulated metres; tangential speed from approximately 1 to 3 m/s, with up to 3.5 m/s during approach. Radial correction tightened to keep the faster circle near the desired radius. Height remains 3.5 m above mapped base within the existing flight band.

Follow-up request: operator offsets display at 10× UWB separation in the Mac simulator with a visible footnote. Raw phone UWB values are unchanged. Display enlargement also means the simulated drone travels the enlarged distances; drone/grid physical sizes are unchanged.

Verification: 84 frontend tests passed, including nearest target/release/staleness, larger orbit tracking and 10× spacing without mutating input. Swift gesture tests passed; final production build and signed iOS build succeeded; whitespace check passed. Installed and launched on the user’s iPhone 15 using devicectl. Real camera and multi-phone UWB behavior still needs live testing. Other phones need the updated build for the revised mapping. Browser-only camera/Python control mappings were not changed; this implementation is for the iPhone demo. Existing Podfile.lock modification excluded.
