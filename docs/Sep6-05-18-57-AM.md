# Preserve close camera while dragging

Completed: 2026-09-06T05:18:57.338910-04:00 (EDT), America/New_York.

Prompt: Clicking and dragging to rotate the phone simulator unexpectedly zooms far out.

Root cause: The new close phone camera stopped updating on pointer-down, while main.ts still ran its legacy mouse-drag handler. That handler called applyOrbitCamera with the Washington Monument target and a 450 m range.

Changes: Guard legacy orbit drag and wheel handlers out of phone mode. Give the phone scene pointer-captured drag controls that retain the current centre and range while changing heading/pitch. Wheel and +/- zoom act on the current distance, with metre-scale limits; Frame group resumes automatic framing. Handle pointer release/cancel/lost capture and restore listeners/touch behavior on disposal. Flight follow behavior remains independent of camera orbit.

Verification: TypeScript/Vite production build passed. All 16 focused checks passed, including a regression that executes the phone scene's pointer and wheel handlers and confirms drag changes orientation without changing centre/range, release ends dragging, wheel zoom preserves the centre, and disposal cleans up. git diff --check passed. No browser interaction QA is claimed. Rebuilt the existing local simulator; refresh it to load this fix. The preceding fist-follow change was also pushed successfully.

Preserved unrelated staged docs/Sep6-03-32-00-AM.md.
