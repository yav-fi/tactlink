# Disable the open/fist/open combo

Completed: 2026-09-05 04:31:26 PM EDT (America/New_York)

## Prompt

Don't have `Open_Palm -> Closed_Fist -> Open_Palm` do anything for now. (User will
test the current build, then we implement the "double finger swing + direction"
wiper properly.)

## What changed

- `config/gesture_sequences.json` - `"sequences": []`. All combos off; no gesture
  gets the `single_delay` hold. The wiper pattern is kept in `__example__` for
  when it's implemented.
- `src/main.py` - demo timeline trimmed to single gestures only:
  `Thumb_Up` (takeoff) -> `Pointing_Up` (orbit) -> `ILoveYou` (return home) ->
  `Thumb_Down` (land).
- README - combos section marked "off right now".

## Verification

- `python -m py_compile src/*.py` clean; `tests/test_sequences.py` 10/10,
  `tests/test_fly_maneuver.py` 6/6 (both use explicit fixtures, not the shipped
  config, so they're unaffected).
- `--demo --check-gestures` log: `takeoff 1.9s, orbit 6.4s, return_home 13.4s,
  land 18.4s` - only single-gesture commands, nothing from combos.
- Rendered `--demo`: takeoff, orbit, return home, land; ends `DISARMED` /
  `MODE HOLD` at origin.

## Active gesture set (for testing)

| Sign | Action |
| --- | --- |
| Thumb up | arm + take off |
| Thumb down | land + disarm |
| Point up | orbit 10 m radius (point again to stop) |
| ILoveYou | return home |

Open palm / fist / Victory: no effect. Combos: none.

## Next

Implement the wiper: train `v_flat`, re-add
`["Victory", "v_flat", "Victory", "v_flat"] -> fly_pointed` to
`config/gesture_sequences.json`. The `fly_pointed` resolution + `fly_*` maneuvers
+ `point_dir` plumbing are already in place from the earlier pass.
