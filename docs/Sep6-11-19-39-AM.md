# Gesture demo: spread the operators out more

Completed: 2026-09-06 11:19:39 AM EDT (America/New_York)

## Prompt

Make the operators more spread out in the scene.

## What changed

- **`flight.py`** `Operators` defaults: scatter `radius` 4.6 -> 7.0 m, `min_sep`
  2.4 -> 4.0 m.
- **`flight.py`** `_scatter`: re-added a gentle camera-azimuth separation
  (`_MIN_AZIMUTH` ~6 deg, `_CAM_EYE_Y` kept in sync with `scene.py`) so that a
  random draw never stacks two operators along the view axis - the earlier pure
  disc scatter occasionally lined 2-3 of them up and they overlapped on screen.
  Fallback path widened to a shallow arc.
- **`scene.py`**: camera pulled back and up (`eye` `(0,-15.5,7.5)` ->
  `(0,-18,9)`, `fov` 62 -> 66) and the ground grid enlarged (`extent` 9 ->
  12, `step` 1.5 -> 2.0) to frame the wider group.

## Verification

- `python -m unittest` (from the folder) - 14 pass. `test_operators_are_random_
  2d_and_spaced` still holds (now >= 4 m apart).
- `--check` loads the model.
- Rendered ~10 random layouts at `--operators 4` and one at `--operators 6`:
  operators fill the frame width with varied depth, none stacked; the full
  scripted `--demo` (dash, targeted + random hand-off, orbit, land) still plays
  correctly over the larger scene.

## Notes

- `_CAM_EYE_Y` in `flight.py` must match `scene.Scene`'s camera `eye` y; both are
  -18.0. If the camera moves, update both.
- Worktree off `origin/main`; shared tree untouched.
