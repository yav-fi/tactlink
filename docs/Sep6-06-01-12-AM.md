# Explain missing custom phone classifier

Completed: 2026-09-06 06:01:12 EDT (America/New_York).

Request: Explain why Ian's custom trained classifier is not running on the phones.

Finding: The phone port integrated Google's stock MediaPipe model and ancillary gesture rules, not the optional PC custom nearest-neighbor classifier. The repository contains its collector, trainer, feature extractor, and prediction code, but models/custom_gestures.npz is neither tracked in the current checkout nor present at its expected local path. This does not establish whether Ian has trained weights on another machine.

This is an incomplete port and missing trained artifact, not an iPhone platform limitation. To reproduce that custom classifier on device, obtain its trained arrays and port the same landmark feature extraction, normalization, distance voting, and rejection behavior. The NPZ is a separate custom format and cannot simply replace Google's gesture_recognizer.task bundle. If the trained artifact cannot be recovered, collect examples and train it again.

Verification: inspected git-tracked model-related paths, checked the expected artifact path, and read the custom classifier's feature-version and NumPy loading code. Documentation only; no application changes, training, or deployment. Preserved unrelated staged documentation and untracked pitch/ work.
