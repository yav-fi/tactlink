# Rear camera gesture input

Completed: 2026-09-06T05:04:01.306028-04:00 (EDT), America/New_York.

Prompt: Investigate gesture None and the rear camera apparently not being used.

Finding: GestureCamera explicitly selected the front wide-angle camera and fed mirrored selfie orientation to Vision. Both phones were already reporting fresh two-phone UWB positions to the server, but no recognized gesture.

Changes: Select the rear wide-angle camera. For this portrait-only app, rotate capture buffers 90 degrees when supported and pass upright orientation to Vision (otherwise use the rear sensor’s portrait orientation). Disable mirroring. Add a rear-camera preview from the same capture session, a processed-frame counter, and clear local recognition status and whole-hand instructions. Preserve existing gesture thresholds and flight actions.

Verification: Native build and signing succeeded (build 1788685393). Existing gesture geometry tests passed. git diff --check passed. Installed and launched the updated app successfully on Alvan’s connected phone with the simulator host configured. It reopens in the lobby and must rejoin Thomas’s current room. Physical rear-camera gesture recognition after this installation has not yet been verified.

Thomas’s current app is com.thomas.SignalMap. This Mac only has the com.arulandu.SignalMap provisioning profile, which excludes Thomas, so his updated installation remains dependent on the signing account/Mac used for his existing app. Asked user for that information; no Thomas update is claimed.

Apple capture rotation reference: https://developer.apple.com/documentation/avfoundation/avcaptureconnection/videorotationangle . Preview frames stay local; no camera images are transmitted.

Preserved unrelated staged docs/Sep6-03-32-00-AM.md.
