# Speech command addition feedback

Completed: September 5, 2026, 05:00:38 PM EDT (America/New_York, UTC-04:00).

Prompt: User reported that Add to mission did nothing after speech-to-text.

Browser inspection showed a correctly transcribed hover/return/land instruction
had already been appended twice after an existing Land. The draft contained 14
commands and failed validation at command 8. Feedback was distant from the
speech controls, so the failure appeared silent.

Added inline status beside Add, a command insertion-position selector, a jump
to the first invalid row, and validation of the complete candidate before any
addition changes the draft. Failed additions retain the transcript and leave
the draft untouched. Successful additions clear the submitted transcript and
show a command count. A revision check rejects outdated asynchronous additions.

Verification: five Node tests passed (three addition regressions and two map
projection tests); JavaScript syntax check passed. Reloaded the browser while
preserving the transcript, retried Add, and verified a visible command-8 error
with the mission still at 14 commands. Existing draft is intentionally retained
for editing. No backend, flight execution, or 3d-mapping files changed.

Commit locally only; the user's instruction requires an explicit request before
pushing or merging planner work.
