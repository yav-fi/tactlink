# README front-facing copy aligned with the TactLink deck

**Completed:** 2026-09-06 11:45:38 EDT (America/New_York)

## Prompt

Read the `Tactlink_DNHacks_White_v4.pdf` pitch deck, then rewrite the front-facing
wording in `README.md` — the mission statement and surrounding copy — so a reader
sees what TactLink means rather than the previous abstract framing. Copy-only change.

## What changed

`README.md`, lines 1–78 (everything above `## Close-up phone demo`):

- Replaced the opening with the deck's promise, **"Direct a drone like a teammate."**
  plus a concrete four-beat description (point → circles you; forward → moves;
  call back → arrives overhead; open palm → stops) and the deck's
  **Gesture → intent → action** line.
- Rewrote the **Mission** paragraph so it leads with the outcome for American teams
  and explains the mechanism in plain language (reads hand signals a squad already
  uses), folding in the old "no time to command stick by stick" line.
- Added **Three design choices** (deliberate commands, spatial meaning, direct
  intervention) from deck slide 02, including the Mach Industries design-input note.
- Added **What works today**: the phones → computer → shared 3D simulation setup,
  a four-row command table (Orbit / Move / Return / Stop) from slide 03, the
  plan-in-2D → visualize-in-3D → interrupt-by-gesture line from slide 04, and the
  in-development spoken-commands note from slide 06.
- Added an **Honest status** paragraph carrying slide 05's disclosure verbatim in
  substance: connected phone prototype, no physical flight data, GPS-independent
  navigation separate, plus the three next validation gates.
- Reframed the workstream list under a **How it is built** heading, rewrote each
  bullet to say what the workstream gives the demo rather than naming it, and
  reordered them to follow the signal path (phones → operators → gestures → 3D →
  planning → distributed runtime → local LLM).

## Fixes made along the way

- The list said "six hackathon workstreams" while listing seven; now says seven.
- The "Phones as live operators" bullet pointed at `#phones-on-the-ground--live-operators-in-the-runtime`,
  which no longer matches any heading. Repointed to
  `#advanced-phones-in-the-separate-distributed-runtime`.

## Verification

- Re-read the spliced file end to end; the header ends cleanly at
  `## Close-up phone demo` with no duplicated or dropped lines.
- Checked every in-document anchor in the workstream list against
  `grep -n '^#\+ ' README.md`: `#local-llm-chat--benchmark`,
  `#webcam-gesture-quadcopter-control`, `#cesium-3d-mapping`,
  `#distributed-mission-runtime`, and the repointed advanced-phones anchor all
  resolve to real headings. `ios/README.md` and `planning/README.md` exist.

## Notes and limitations

- Copy only. No code, config, or command in the README was changed, and every
  technical section from `## Close-up phone demo` down is byte-identical.
- The claims added are the deck's own, including its caveats; nothing new is
  asserted about hardware flight.
