# requirements.txt + drop dead mlx-vlm dependency

Completed: 2026-09-05 12:25:23 PM EDT

## Prompt

Confirm the README's dependency install step is complete/accurate for a new teammate
pulling the repo, and add a requirements.txt so it's one standard command instead of a
copy-pasted pip line.

## What changed

- Found and fixed a real inaccuracy: the setup line listed `mlx-vlm`, but nothing in the
  project actually calls it anymore (leftover from before the chat backend existed —
  `chat_client.py` imports `transformers` directly for the chat template). Replaced with
  `transformers`.
- Added `requirements.txt` (`huggingface_hub`, `transformers`, `uv`, `chad-code`).
  README's setup step is now `pip install -r requirements.txt`.

## Verification

- Confirmed via `grep` that `mlx_vlm`/`mlx-vlm` has zero references left in any `.py` or
  `.sh` file in the project.
- Ran `pip install --dry-run -r requirements.txt`: resolves cleanly, would install
  `chad-code-2.0.2`, `transformers-5.12.1`, `uv-0.12.10`, `huggingface_hub-1.30.0` plus
  their own dependencies (mlx, mlx-lm, mlx-metal, sentencepiece, etc.) with no conflicts.

## Notes, assumptions, limitations

- `requirements.txt` has no pinned versions — acceptable for a hackathon project moving
  fast, but means a teammate's exact installed versions could drift from what's been
  tested here (currently `chad-code` 2.0.2, `transformers` 5.12.1).
- `~/mlx-bench` still needs a separate `git clone` (not a pip package) — it's imported by
  path (`sys.path.insert`), not installed; this is unchanged and still documented as its
  own step in the README.
