# Identify the working PC recognition engine

Completed: 2026-09-06 06:11:14 EDT (America/New_York).

Request: Thomas recalls PC gesture recognition working roughly two hours earlier; explain whether that used MediaPipe or something different.

Both PC camera paths in the repository use Google's MediaPipe GestureRecognizer. Python src/hand_tracker.py uses its pretrained float16 v1 task bundle, with optional local custom nearest-neighbor fallback only below canned confidence 0.62. The browser laptop-camera path in 3d-mapping/src/gesture-camera.ts uses @mediapipe/tasks-vision and public/models/gesture_recognizer.task, previously verified byte-identical to the iPhone model. Python's custom three-finger and two-finger swing actions use landmark geometry separately.

Git history shows no changes to the queried Python detector/interpreter files during approximately 03:30–05:00 EDT today. Browser gesture files last changed around 02:30 EDT. These facts identify repository code, not the exact command, branch, local weights, or camera feed used in Thomas's remembered session.

The optional NPZ is not necessary for the PC's standard MediaPipe gesture recognition. Its absence alone does not explain poor phone results. Current iPhone processing differs in camera input, geometric overrides and temporal filtering despite sharing the base model. A comparison of raw PC and phone landmarks/predictions versus final labels can separate camera/preprocessing problems from override/control logic problems before considering retraining.

Verification: inspected current Python and browser imports/model paths and time-filtered Git history. Documentation only; no code changes or runtime reproduction. Preserved unrelated staged documentation.
