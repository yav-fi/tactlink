# Three-finger detection: match on pinky-down, not thumb-in

Completed: 2026-09-05 07:18:11 PM EDT (America/New_York)

## Prompt

Still not recognised. User's `--check-gestures` console log showed the real
detection: holding three fingers sideways reads as `fingers=TIMR-` almost every
frame - **the thumb registers as extended** (it sticks out when the other three
point sideways), while pinky is reliably down.

## Cause

The previous pass required `not f[0]` (thumb tucked). With `fingers=TIMR-` that
made `_three_fingers` False on every frame.

## What changed - `src/finger_swing.py`

- `_three_fingers` -> `f[1] and f[2] and f[3] and not f[4]` (index+middle+ring
  up, **pinky down**; thumb ignored). Distinct from Victory (2), open palm
  (pinky up too), pointing (1).
- `_is_sideways` now also rejects pointing straight *down*
  (`42 deg <= angle <= 138 deg`).

## Verification

- Replayed the user's actual frames: `TIMR-` sideways -> `3finger=True
  sideways=True` (fires); `TIMRP` open palm -> False; `TIM--` Victory -> False;
  `TIMR-` pointing up -> `sideways=False` (won't fire). Correct on all.
- `python -m py_compile src/*.py tests/*.py` clean; all suites green
  (operators 5, finger_swing 8, fly_maneuver 10, swarm 5, sequences 10).
- Not run on a live camera here; the user's logged frames are the ground truth.

## For the user

`git pull`, `python src/main.py --check-gestures`, hold three fingers sideways -
the console should now show `3finger=True sideways=True`, and after ~0.35 s
`fly_bearing:...` appears in the `fired:` list.
