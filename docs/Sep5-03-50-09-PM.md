# Fix fatal manual-flight undo

Completed: 2026-09-05 15:50:09 EDT (America/New_York).

Request: debug intermittent fatal errors when undoing manual flight.

Reproduced in the live Google map: deploy Normal drone, take manual control, then Undo manual flight. Cesium stopped rendering in GeographicProjection.project with an undefined longitude while updating primitive bounding spheres. First-flight undo restores an empty path; hidden geometry previously included undefined origin-marker positions and an Earth-center crash-marker position.

Initialize hidden origin/crash markers at the valid deployment coordinate. Restore the origin marker to the first route point or home when the route is empty. Empty hidden trails retain valid geographic callback points while the underlying recorded route stays empty, avoiding invalid transient geometry during removal/restoration.

Verification: reproduced fatal error before the fix; repeated the live sequence after it and confirmed successful undo, hovering state and interactive orbit camera with no new render error. All 27 tests and TypeScript/Vite build passed. New regression covers three first-flight undo cycles for Normal and Survey drones, valid hidden geometry, resumed movement and cleanup.
