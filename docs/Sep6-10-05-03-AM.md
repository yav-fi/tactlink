# Phone demo gestures and feedback

Completed 2026-09-06T10:05:03.485597-04:00 (America/New_York).

Request: preserve the existing UI style, offer only two- and three-phone iOS rooms while retaining five-phone code, add overhead orbit/hover gestures, and explain blocked movement.

Changes:
- Default to two phones, with a segmented two/three selector inside the original card. Legacy five-phone selector and messages remain in comments; old room codes/settings cannot select that mode. Shared legacy geometry/protocol code remains intact.
- Three extended fingers (index/middle/ring) request a 2 m radius orbit; four request hover overhead. Both use 3.5 m above the operator's mapped base, constrained to the existing flight band, and stop moving on release. Fist follow and one/two-finger climb/descent remain.
- Orbit steering preserves acceleration across heading updates. Other gesture motion keeps its existing behavior.
- Fresh simultaneous claims stop with conflict feedback. Status panels explain missing/stale UWB, missing feeds, short holds, confidence, ownership, pause, height limits, obstacles and arrival overhead.
- Existing mint palette, typography, card styling, camera preview and panel structure retained; no CSS or redesign.

Verification: 81 frontend tests passed; production TypeScript/Vite build passed; Swift gesture tests including rotated three/four-finger poses passed; 79 room/protocol checks passed; signed generic iOS device build succeeded; git diff whitespace check passed.

Limitations: no live multi-phone RF/camera acceptance test or device installation performed in this turn. Phone shows local readiness; actual execution explanations live on Mac because the existing UDP phone feed is one-way. Two-phone direction remains assumed; three-phone geometry remains flat. Heights refer to mapped base, not a measured head position. Existing unrelated ios/Podfile.lock change excluded.
