# Surface-limited Survey view

Completed: 2026-09-05 15:29:52 EDT (America/New_York).

Request: extend the Survey cone and coverage until they encounter map surfaces instead of using fixed range.

Replaced the fixed cylinder with a twelve-sided sampled cone. Center and edge rays query loaded map geometry while excluding application entities; nearest terrain or mesh hits end rays. Missing hits display up to a 10 km rendering limit and create no coverage. Coverage triangles use actual sampled surface positions instead of flattening fixed-range footprints onto the map. Sampling is staggered across drones, at most twice per second per drone; the underside apex follows every frame. Existing coverage cap, reset and undo remain supported.

Verification: automated surface-plane, missing-hit, underside-mount, color and coverage/undo tests; TypeScript/Vite build passed. No live map/render performance validation. Surface interpolation between thirteen rays remains approximate: thin obstacles, discontinuities and unloaded geometry can be missed. No exhaustive visibility guarantee.
