# Gesture demo: drop point-to-switch-operator, keep Victory

Completed: 2026-09-06 11:41:31 AM EDT (America/New_York)

## Prompt

The point-at-an-operator hand-off wasn't working well; remove it. Keep the
Victory-hold (random) hand-off. (The two-finger wiper dash stays.)

## What changed

- **`flight.py`** — removed `Operators.aim_from_point` / `aim` / `aim_progress`
  / `commit_aim` and the `target` / `_aim_*` state; removed the `handoff_aim`
  autopilot command and the `AIM_*` constants. `random_handoff` (Victory) and
  `dash_east` / `dash_west` (wiper) unchanged.
- **`gesture_demo.py`** — `Sim.advance` no longer runs the aim/dwell logic or
  takes `force_aim`; `hud` drops the `aim` key. Scripted `_DEMO` replaces the
  `aim:2` segment with a second Victory hold; `_demo_step` no longer returns a
  force-aim.
- **`scene.py`** — removed the orange target ring, the `_TARGET` colour, and the
  `aim:` HUD line; bottom hint now reads
  "Victory ~2 s -> hand off to a random operator | two-finger wiper -> dash L/R".
- **`test_gesture_demo.py`** — `AimHandoffTests` -> `RandomHandoffTests` (13
  tests total).
- README updated.

## Verification

- `python -m unittest` (from the folder) — 13 pass.
- `--demo --headless` plays the full sequence: take off -> climb -> orbit ->
  halt -> wiper dash right -> wiper dash left -> Victory hold -> random hand-off
  + fly there -> orbit -> return -> Victory hold -> hand off again -> land.
- Live webcam not testable here; recognizer calls unchanged.

## Notes

- Worktree off `origin/main`; shared tree untouched.
