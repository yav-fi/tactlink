# Batch piloting

Completed: 2026-09-05 14:34:00 EDT (America/New_York).

Request: Control a batch like an individual drone, release and pick it up with markers and preserved paths, and set speed for the whole batch.

Added shared manual movement and heading for selected drones, a shared positive decimal mph setting, batch camera framing, release prisms and clickable batch pickup markers. Pickup retains current positions and routes. Batch members share color; marker colors follow the lead member. Fixed trail sampling so resumed movement preserves the exact release endpoint. Reset all clears batch state and markers.

Verification: all 14 automated tests pass, including ten-drone speed/spacing, release/pickup, path continuation, unrelated drone isolation, atomic speed validation and cleanup. TypeScript and Vite production build pass. Browser startup confirms new controls and empty fleet state; map interaction was not visually verified.

Batch movement translates the formation using each member's local geographic axes; it does not rotate formation offsets or add collision avoidance. Historical markers recall the same members at their current positions.
