# Missing second phone feed

Completed: 2026-09-06T05:23:41.149096-04:00 (EDT), America/New_York.

Prompt: The iPhone room reports two members but the simulator shows only one of two connected.

Findings: Both phones' current diagnostics confirmed the same room fingerprint, two-phone mode, 2/2 members, and successful real UWB geometry. The receiver initially showed only Alvan's fresh UDP sample, which itself reported members=2. This Mac's Wi-Fi address is 192.168.1.115. Thomas's app preferences instead contained room.simBridge=192.168.1.200:9870. His feed therefore targets a different host. His current app uses com.thomas.SignalMap, build 5. User instructed to change Thomas's Visualizer host to 192.168.1.115:9870 and rejoin the same room.

Changes: The browser now labels the phone-reported room count separately from the number of feeds reaching this Mac. When the reported room has more people than incoming feeds, it explains the visualizer-destination check. It does not fabricate a second avatar or gesture stream from the membership count.

Verification: Read each phone's app-specific diagnostics and Thomas's saved host, checked the current Mac Wi-Fi address and live /api/phones response. TypeScript/Vite production build and git diff --check passed. The last receiver check still contained only Alvan's sample (then becoming stale); Thomas's corrected feed has not yet been verified. No iOS code changes or reinstall were required.

Preserved unrelated staged docs/Sep6-03-32-00-AM.md.
