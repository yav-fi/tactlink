# Split performance table into model + plug-in algorithms

Completed: 2026-09-05 12:42:55 PM EDT

## Prompt

Split the Performance table's "Model" column into the base model (linked to its
Hugging Face page) and a separate "Plug-in Algorithms" column listing DFlash2 and
LibraSpec.

## What changed

- `README.md`: Performance table is now `Hardware | Model | Plug-in Algorithms | Decode
  tok/s`. Model links directly to
  https://huggingface.co/mlx-community/Qwen3.8-27B-4bit; Plug-in Algorithms lists
  "DFlash2 (w4:gs64 draft), LibraSpec" — matching what's actually running (confirmed via
  the live server process's own environment: `CHAD_LIBRASPEC=1`, `CHAD_LIBRASPEC_ALPHA=8.0`,
  loaded from the LibraSpec-enabled fork, not the public package).

## Verification

- Read the edited table back to confirm rendering (link syntax, column count matches
  header separator row).

## Notes, assumptions, limitations

- Single row for now (this machine/config only), same as before — just restructured
  columns, no new data.
