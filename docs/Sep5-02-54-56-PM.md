# Fix collision movement lock

Timestamp: 2026-09-05 14:54:56 EDT (America/New_York); verification continued afterward.

Request: reassess collision logic because drones no longer move.

Found the hidden fallback globe imposed a zero-ellipsoid-height floor even when Google tiles were active. Live placement above the National Mall produced a valid -11 m ellipsoid height, below this incorrect floor. Skip the hidden globe and retain tile-mesh checks. Also separate ray-query exceptions from collisions, restore Cesium's original render view after a failing offscreen query, surface query warnings, and allow low-clearance drones to climb out. Actual reported hits continue to stop movement.

Verification: 18 automated tests and TypeScript/Vite production build pass. Added negative-height photorealistic movement, failed-query batch movement/render-view restoration, and low-clearance escape regressions. Live Google-map deployment at -11 m successfully moved via the mission controller to latitude 38.89140 and altitude 0 m. Automated keyboard taps were too short to establish live sustained WASD movement. A negative-altitude mission descent was rejected by the existing mission validator, so live ground-impact testing was not completed.

Limitations: collision queries still depend on rendered tile geometry. When a query fails, movement continues with a visible warning and any remaining available ground checks; this is a simulator fallback, not guaranteed collision protection.
