# Console finger-state logging in --check-gestures

Completed: 2026-09-05 07:14:49 PM EDT (America/New_York)

## Prompt

Three-finger gesture still not recognised after the last loosening pass. Need to
see what the tracker is actually detecting.

## What changed

- `src/main.py` - in `--check-gestures`, twice a second while a hand is visible,
  print a line to the console:
  `hand: fingers=TIMRP pointdir=(x,y) orient=V/H | gesture=... 3finger=bool
  sideways=bool 2finger=bool`
  so a live pose can be diagnosed from the terminal (the on-panel readout is easy
  to miss).

## Verification

- `python -m py_compile src/main.py` clean.
- `--demo --check-gestures` prints the line (shows `-----` there because the demo
  hand does not populate finger states outside the swing/three windows).

## Next

User runs `python src/main.py --check-gestures`, holds three fingers sideways,
and pastes the `hand:` lines. That says whether the failure is finger detection
(`fingers` wrong), orientation (`sideways=False`), or something downstream.
