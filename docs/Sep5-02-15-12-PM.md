# Simplify mapping UI and discuss group commands

Completed: 2026-09-05 14:15:12 EDT (America/New_York).

Request: remove plain-English flight instructions and the Free camera button; discuss, but do not implement, commands for groups of roughly ten drones.

Removed the flight instruction textbox, preview button/status and browser bridge request handler. Removed the Free camera button and its listener. Camera navigation, deployment, pilot camera modes and structured mission JSON remain available. Existing standalone preview conversion code and backend work were left intact. No group-command feature was implemented.

Verification: TypeScript and production build passed; git diff --check passed. Group-command proposal: select drone IDs, save named groups, click a destination with altitude and spacing, allocate distinct destination slots and dispatch per-drone missions on a shared start time. Formation tracking and collision avoidance would be separate later work.
