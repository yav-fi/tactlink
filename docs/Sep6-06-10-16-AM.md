# Search Git history for custom gesture weights

Completed: 2026-09-06 06:10:16 EDT (America/New_York).

Request: Dig through Git history and model folders to recover Ian's custom trained classifier.

## Findings

No recoverable models/custom_gestures.npz or raw gesture NPY recordings were found. The local root models/ and data/ directories are absent. This checkout is not shallow.

The tracked 3d-mapping/public/models/gesture_recognizer.task is Google's standard model, byte-identical by SHA-256 to the bundled iPhone model (97952348cf6a6a4915c2ea1496b4b37ebabc50cbbf80571435643c455f2b0482). Other historical model assets were drone GLB meshes, not gesture weights.

Ian's initial commit 6c56215 added the collector/trainer/predictor code. Its docs/Sep5-12-59-44-PM.md explicitly says synthetic training data and a synthetic test NPZ were deleted before commit and no real model shipped then. Gitignore explicitly allowed models/custom_gestures.npz to be committed, so blanket model ignoring alone does not explain its absence.

Later documentation establishes that a local trained model did exist. docs/Sep5-05-01-23-PM.md describes training two_up and two_flat and their misclassification of a one-finger point. docs/Sep5-05-31-47-PM.md says the custom classifier was overriding other gestures and documents replacement of its wiper detection with geometry. docs/Sep5-07-05-42-PM.md (commit f9b9983) reports a loaded local model with labels 1-finger, two_up, and two_flat, no other class, and recommends deleting it because it could interfere with Pointing_Up. These notes are historical evidence, not proof of the current state of Ian's machine.

## Search and verification

- Fetched both configured remotes and tags. The arulandu remote advertised no branch/tag heads.
- Fetched all five origin pull-request heads into remote-tracking refs, leaving the current branch and checkout unchanged.
- Searched all reachable branch/tag/PR/reflog object paths and commit history for models/, NPZ, NPY, and gesture model formats; no custom weights or recorded samples.
- Inspected 74 unreachable blobs for NumPy/ZIP archive signatures and 100 unreachable trees for model paths. No custom-model candidate was recoverable; no lost-found files or Git history changes were created.
- Read the original and later training notes to distinguish missing repository artifacts from evidence of local training.

No application changes, model recovery, or deployment. Only this prompt note is committed. Preserved unrelated staged docs/Sep6-03-32-00-AM.md and teammate changes already present on main.

Next useful source is Ian's original PC checkout or a backup of models/custom_gestures.npz or data/gestures/. The old model was not documented as a reliable fist/up/down controller, so obtaining it would enable inspection rather than guarantee improved recognition.
