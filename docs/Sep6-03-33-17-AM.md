# Prepare phone installation and integrated runtime

Completed: 2026-09-06 03:33:17 EDT (-0400), America/New_York.

## Prompt

Tell the user when ready so they can connect five phones for app installation.

## Work performed

- Installed missing Python runtime/test dependencies into the ignored .venv and frontend dependencies into ignored node_modules using the existing launcher.
- Started `PHONE_FEED_HOST=0.0.0.0 SIMULATION_DRONE_COUNT=5 ./start --no-open`; backend and frontend remain running for the hardware test.
- Built and signed SignalMap through `./ios/scripts/dev ship`; published local build 1788679936, fingerprint prefix 11f30210. No app source changed.
- Started `./ios/scripts/dev watch` for installation as paired physical phones become reachable. Alvan's iPhone installed successfully and launched. Ian's phone was listed as paired/available but installation was not yet reported. Yav's phone was listed unavailable.
- Queued the runtime browser view at http://127.0.0.1:5173/?mode=runtime.
- Mac Wi-Fi address at inspection: 192.168.1.115; Visualizer host should be 192.168.1.115:9870 on each phone.
- Removed the incidental npm install peer-metadata changes from package-lock.json. Preserved another task's staged documentation.

## Verification

- Signed iOS build/publish completed successfully; Alvan's installation and launch confirmed by deployment watcher.
- Frontend TypeScript check and Vite production build passed through the launcher.
- `.venv/bin/python -m pytest tests/test_operator_positions.py -q`: all 25 tests passed, with dependency deprecation warnings.
- HTTP state endpoint returned five drones and zero operators; frontend returned HTTP 200.
- `lsof` confirmed backend listening on UDP *:9870.
- Initial sandboxed dependency install, signing, and device discovery could not access required networking/macOS services. Retrying with approved escalated execution succeeded.

## Remaining hardware work

Connect and trust the remaining phones, enable Developer Mode if required, and grant app permissions. On the same Wi-Fi, use one shared five-phone room with the three-phone toggle off and each phone's Visualizer host configured. Wait for the UWB group map, then verify five real operators and gesture-driven missions in runtime mode. Full five-phone hardware success has not yet been established. The optional mission AI model is absent and is not required for this gesture test.
