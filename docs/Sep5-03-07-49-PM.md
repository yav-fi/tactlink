# Testing the merged 3D environment

- **Completed:** 2026-09-05 03:07:49 PM EDT (-0400)
- **Prompt:** Explain how to test the 3D environment merged from Thomas's branch.

## Summary

Reviewed the merged `3d-mapping` documentation, package scripts, environment
template, and root runtime instructions. Identified the browser-only local
sandbox and the backend-connected runtime as the two supported ways to test the
Cesium environment.

## Changes

- Added this prompt record only; no application code was changed.

## Verification

- Ran `npm --prefix 3d-mapping test`: all 9 tests passed.
- Ran `npm --prefix 3d-mapping run build`: TypeScript checking and the Vite
  production build completed successfully.

## Notes

- The installed Node.js version is 20.13.0. Vite warned that it requires Node.js
  20.19+ or 22.12+, although the build completed successfully. Upgrading Node is
  recommended before testing.
- Google Photorealistic 3D Tiles require a Cesium ion token in
  `3d-mapping/.env`. Without one, the app uses Cesium's fallback globe.
- Existing unrelated edits in `scripts/benchmark.py` and
  `scripts/benchmark.sh` were left untouched.
