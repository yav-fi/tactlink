# Bulk deployment and shared-color group commands

Completed: 2026-09-05 14:23:44 EDT (America/New_York).

Requests: push UI cleanup; retain easy individual deployment, add nearby bulk deployment, implement strategy-game-style multi-drone selection/groups/destination commands; drones commanded together should share a color.

Pushed cleanup commit 0afae94 to origin/3d-mapping-2. Added grid deployment with default ten drones and 30-meter spacing, configurable up to 100 per batch. Single deployment remains its own button. Added checkboxes, select-all/clear and map-marker selection, named saved groups, and destination picking with height/spacing plus dashed route previews. Group commands snapshot members, assign distinct slots and launch together at individual speeds. Actual drone positions advance, enabling later individual control from the destination. Group commands replace selected routes; playback remains reusable. Shared colors update existing markers and are used for future markers. IDs remain visible. Reset all clears groups and drone entities; Stop playback stops group movement. Group names and membership are session-local. No collision avoidance or in-transit formation constraints are claimed.

Verification: production build and TypeScript passed. All twelve tests passed, including a ten-drone grid, minimum spacing, individual movement isolation, invalid batch inputs, group saving/color consistency, simultaneous movement at different speeds, exact separate arrivals, unaffected nonmembers, ad-hoc shared color, stopping, reset cleanup and existing replay/speed behavior. git diff --check passed. This turn's new UI has not been browser-verified. Existing untracked .vite cache was preserved and excluded from staging.
