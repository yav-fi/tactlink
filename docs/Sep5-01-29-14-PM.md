# Multi-drone deployment

Completed: 2026-09-05 13:29:14 EDT (America/New_York).

Request: shrink the leading arrow, replace release spheres with prisms, start empty, support free-camera placement of multiple drones with eight cycling colors, and reset all drones.

Live arrows are three meters long instead of 22. Release markers use 16 x 10 x 4 meter boxes. Fleet assigns unique IDs and cycles eight colors; all per-drone entities use its ID and color. Deployment offers surface picking, height above the picked surface, preview, confirmation and cancellation without recording a route. Selection routes manual control and updates mission targeting. Reset all removes all fleet entities and restarts the palette. Existing camera modes and persistent routes remain available per drone. Removed the blocking terrain initialization so map controls initialize immediately.

Verification: browser confirmed empty startup, surface picking, confirmation, cyan and orange drones at separate starting points, and selected-drone takeover. Automated tests verified nine deployments/color wrap, independent movement, no placement routes, release prisms, trail preservation and cleanup without removing unrelated map entities. npm run build and git diff --check passed.
