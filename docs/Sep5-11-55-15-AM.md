# Chat template fix + Ollama research

Completed: 2026-09-05 11:55:15 AM EDT

## Prompt

Investigate why `chat_client.py`/raw curl against `/completion` returned an unrelated
ramble instead of an assistant-style reply to "hello", and fix it in the least invasive
way possible so it works out of the box for teammates. Separately: research whether
chad's engine can be used through Ollama, or made Ollama-compatible.

## What changed

- `chat_client.py`: added `chat()`/`chat_sync()`, which apply the target model's own
  chat template (via `transformers.AutoTokenizer.apply_chat_template`, loaded from the
  local model dir, `enable_thinking=False`) before sending to `/completion`. The
  existing `complete()`/`complete_sync()` are unchanged and still send raw prompts
  as-is. No new dependency: `transformers` is already pulled in transitively by
  `mlx-vlm`, which the model-setup instructions already have teammates install.
- `README.md`: switched the usage example to `chat_sync`, explained the
  raw-vs-templated distinction, and documented the new `CHAT_MODEL_DIR` env var.

## Verification

- Confirmed the root cause directly: `/completion` is a raw llama.cpp-style completion
  endpoint (confirmed by reading chad's `serve.py`/`parse_completion_request`) — a bare
  string prompt gets tokenized and continued with no chat template, so "hello" produced
  an unrelated continuation, not a reply. This was expected behavior, not a bug in the
  server.
- Rendered the chat template standalone first (`tokenizer.apply_chat_template(...)`) to
  confirm it produces the expected `<|im_start|>...` turns before wiring it into the
  client.
- Restarted `chad serve` (mlx-community 4bit target + w4:gs64 DFlash2 draft +
  LibraSpec) and called the new `chat_sync("hello")` through it end-to-end: got
  `"Hello! How can I help you today?"` — a real assistant reply, confirming the fix
  works against the live server.

## Notes, assumptions, limitations

- Ollama researched (web search) and found NOT to be a viable host for chad's engine:
  Ollama's own MLX backend (added 2026, v0.19+) is Ollama's own implementation, not a
  public plugin slot for third-party engines; chad's docs only mention Ollama as a
  competing benchmark baseline. A thin Ollama-API-compatible shim (translating
  `/api/chat` to chad's `/completion`, the same pattern as existing tools like
  `ollama-openai-proxy`) would be feasible but was not built — not yet requested.
- `enable_thinking=False` is hardcoded in `_render_chat`; a caller wanting the model's
  reasoning/thinking mode would need a code change, not just a parameter, today.
