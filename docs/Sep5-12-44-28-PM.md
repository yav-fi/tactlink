# Faster camera and manual drone control

Completed: 2026-09-05 12:44:28 EDT (America/New_York).

Request: increase WASD sensitivity and add a button to take control of the drone.

WASD camera translation is now three times faster; R/F camera speed is unchanged. Control drone toggles direct manual flight at 45 meters per second using W/S north/south, A/D west/east, R/F up/down. The camera follows from the south. Diagonal input is normalized. Taking control cancels the current mission; releasing control leaves the drone hovering. Reset, mission execution and camera mode buttons exit manual control. Manual movement uses real elapsed time.

Verification: npm run build (TypeScript and production build) and git diff --check passed. Live browser input has not been verified. No collision avoidance is implemented.

Changes remain local; the preceding automatic approval review blocked the commit/push workflow pending explicit authorization for the GitHub destination.
