# Simplify README to setup + 3 commands

Completed: 2026-09-05 12:22:27 PM EDT

## Prompt

Trim the README down to just setup plus exactly three commands (start server, ask,
benchmark) — short, no over-explaining. Identify and remove redundancy.

## What changed

- Collapsed "Model setup" and "Chat backend" into one `## Setup` section (install +
  clone + the two one-time build/download scripts), and added a `## Commands` section
  listing exactly `start_chat_server.sh`, `ask.sh`, `benchmark.sh` with one-line comments.
- Removed: a dead `mlx_vlm.generate` example (nothing in the project actually calls it —
  leftover from before the chat backend existed), a duplicate startup explanation (the
  manual `pip install && build_dflash_draft.sh && start_chat_server.sh` block was
  redundant with the setup section once split out), inline Python usage examples for
  `chat_sync`/`Conversation` (covered by `chat_client.py`'s own docstrings), and design
  rationale (LibraSpec fallback behavior, benchmark methodology, thermal gate details) —
  that history already lives in `docs/`, so the README doesn't need to re-explain it.

## Verification

- Read the full resulting README top to bottom: setup block is copy-pasteable in order,
  the three commands section matches the three scripts that actually exist
  (`start_chat_server.sh`, `ask.sh`, `benchmark.sh`).

## Notes, assumptions, limitations

- Env var overrides (`CHAT_SERVER_URL`, `CHAT_MODEL_DIR`, `MLX_BENCH_DIR`, benchmark
  flags) are no longer documented in the README — they still work (unchanged in code),
  just no longer spelled out here per the "short, no over-explaining" ask. Someone
  needing them can read the scripts' own `--help`/comments.
