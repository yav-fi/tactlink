# Handoff runbook for the remaining phone-gesture work

Completed: 2026-09-05 10:51:10 PM EDT (America/New_York)

## Prompt

Write down what's left to do for the iPhone → simulator integration and push it,
formatted so a teammate's coding agent can execute every command.

## What changed

- **`docs/phone-gestures-REMAINING.md`** (new). A runbook:
  - current state table (done / not done) and repo map
  - the UDP wire format and gesture → action mapping
  - **Task 1** — verify the Python side (commands + accept criteria)
  - **Task 2** — verify iOS compiles + deploys on a Mac; confirm the compass
    conventions with a 2-minute on-phone check
  - **Task 3** — on-phone gesture recognition (MediaPipe Tasks for iOS): the
    CocoaPods/workspace decision, `Podfile`, model download URL, Info.plist key,
    a spec for `GestureCamera.swift`, and the exact `RoomSession` /
    `RoomBridge` / pbxproj wiring (mirrors how `HeadingSource` is already wired)
  - **Task 4** — field calibration with real phones
  - **Task 5** — optional polish
  - full demo acceptance checklist + troubleshooting table
- **`README.md`** — link to the runbook from the Operators section.

## Verification

- Markdown only; no code changed. `python tests/test_phone_feed.py` /
  `test_operator_gestures.py` / `test_operators.py` still `13/7/6` green.
- Commands in the runbook were cross-checked against the actual CLI
  (`python src/main.py --help` shows `--phone-feed` / `--phone-frame-rot` /
  `--no-camera`), the actual iOS scripts (`ios/scripts/{test-protocol,test-room,
  dev}`), and the actual `.task` model URL used by `src/hand_tracker.py`.

## Notes

- Task 3 is the only substantial remaining work and is Mac-only. The runbook
  flags the MediaPipe-CocoaPods friction and names Apple Vision as the
  lower-risk fallback, leaving the final call to the human.
- Done in a worktree off `origin/main`; shared working tree untouched.
