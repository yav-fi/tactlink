# Merge mission and planning runtime into main

Completed: 2026-09-05 12:57:39 PM EDT (America/New_York)

## Prompt

Create, approve, and merge a pull request from `yavin/drones` into `main`, reporting any discrepancies.

## What changed

- Fetched the latest `origin/main` and compared it with `yavin/drones`.
- Merged `origin/main` into the feature branch before opening the pull request.
- Resolved the two predicted additive conflicts without discarding either workstream: `.gitignore` now includes simulator/test and LLM/gesture artifacts, and `README.md` retains all setup instructions while presenting the four current workstreams.
- Included the branch's mission simulation runtime, autonomous planning engine, server, tests, demo, and documentation alongside the latest local-LLM and gesture-control work from `main`.

## Verification

- All 18 planning, simulation, API, and WebSocket tests passed after conflict resolution.
- Python byte-compilation passed for `planning`, `simulation`, `server`, `src`, and `tests`.
- The mission demo completed: Drone 2 failed, Drone 4 was reassigned, and final mission capability recovered to 100%.
- `git diff --check` found no whitespace errors.

## Discrepancies and notes

- The branch contained `a184543 add planning engine` in addition to the previously delivered mission-runtime commits; it is intentionally included in this integration.
- The latest `main` had advanced with local-LLM and gesture-control commits. They were incorporated into the feature branch before PR creation.
- Dependency setup remains split: LLM/gesture dependencies use `requirements.txt`, while planning/simulation/server use `pyproject.toml`. Both are preserved because consolidating dependency management was outside this merge request.
- The test suite reports two third-party Starlette/FastAPI test-client deprecation warnings; all tests pass.
