# Reliable demo launcher

Completed: 2026-09-05 12:13:15 PM EDT (America/New_York)

## Prompt

Resolve the demo startup failure where `python3 -m simulation.demo` used the system Python and raised `ModuleNotFoundError: No module named 'pydantic'`; the shell also had no global `python` command.

## What changed

- Added `scripts/demo`, an executable project launcher that always runs the demo with `.venv/bin/python` regardless of shell activation or global Python aliases.
- Added a clear setup message when `.venv` does not exist.
- Updated the README to recommend `./scripts/demo` and documented the direct `.venv/bin/python` troubleshooting command.

## Verification

- Confirmed `.venv/bin/python` imports Pydantic 2.13.5.
- Ran `./scripts/demo` successfully through the full scenario.
- Observed Drone 2 fail at 10.0s, Drone 4 receive the reassignment at 12.9s, and final mission capability reach 100%.

## Notes

- Dependencies remain isolated in the project virtual environment; nothing is installed into the system Python.
- The unrelated untracked `planning/` directory remains untouched and excluded.
