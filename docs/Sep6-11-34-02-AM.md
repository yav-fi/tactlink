# Point repository links at the renamed repo

**Completed:** 2026-09-06 11:34:02 EDT (America/New_York)

## Prompt

The repo moved to `https://github.com/yav-fi/tactlink`. Update the README clone
command and any other line carrying the repository link.

## What changed

Three live references to `yav-fi/dnhacks26`:

- `README.md:14` — the `git clone` command.
- `pitch/app/page.tsx:23` — `REPO`, the base for every source-file link on the
  pitch site.
- `pitch/app/page.tsx:1106` — the "Explore the implementation" link.

## Deliberately left alone

Other `dnhacks26` strings are not repository links, and changing them would
break things:

- `simulation/authentication.py:17` — a salt inside a SHA-256 key derivation.
  Changing it changes every derived key and would break room authentication
  against already-installed phone builds.
- `pyproject.toml:6` — the Python package name `dnhacks26-runtime`, which the
  installed egg-info and imports resolve against.
- `dnhacks26.arulandu.com` in `pitch/README.md` and `pitch/app/layout.tsx` — the
  deployed domain, which is separate from the GitHub repository name and was not
  part of this request.
- `3d-mapping/scripts/generate-uav-glb.mjs:253` — a glTF `generator` metadata
  string in already-produced assets.
- `docs/` — historical prompt records. One entry quotes the old URL as part of
  what was asked at the time; rewriting it would falsify the log.

## Verification

- `grep` confirms no `github.com/yav-fi/dnhacks26` remains outside `docs/`.
- `npx tsc --noEmit` in `pitch/` reports no errors.

## Notes

The local git remote still points at the old URL. GitHub redirects renamed
repositories, so pushing keeps working, but it can be updated with:

```sh
git remote set-url origin https://github.com/yav-fi/tactlink.git
```
