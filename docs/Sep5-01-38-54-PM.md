# Merge and reconcile 3D mapping

- Completed: September 5, 2026 at 1:38:54 PM EDT (UTC-04:00)
- Prompt: Merge `thomas/3d-mapping` into current `main`, resolve discrepancies, keep multiple web frontends runnable, and verify the merged branch.

## What changed

- Fast-forwarded local `main` to the latest `origin/main` and merged the latest `origin/thomas/3d-mapping` without conflicts.
- Preserved Thomas's full Cesium fleet deployment, camera/piloting, route rendering, local missions, configurable speeds, tests, and documentation.
- Assigned the Cesium development server explicitly to `127.0.0.1:5173` and its production preview to `127.0.0.1:4173`, both with strict port handling; the FastAPI runtime remains on `127.0.0.1:8000`.
- Updated the root and 3D mapping documentation with parallel startup commands, URLs, and a clear boundary between the browser-local mission format and canonical runtime `simulation.models.MissionCommand`.
- Corrected stale gesture-runtime documentation so it matches the runtime status-panel behavior.

## Verification

- `.venv/bin/python -m pytest`: 39 passed, with one dependency deprecation warning.
- `npm test` in `3d-mapping/`: 5 passed.
- `npm run build` in `3d-mapping/`: TypeScript check and Vite production build passed.
- Vite development server: HTTP 200 from `http://127.0.0.1:5173`.
- Vite production preview: HTTP 200 from `http://127.0.0.1:4173`.
- Existing FastAPI runtime: live state returned from `http://127.0.0.1:8000/api/state`.

## Notes

- The two web frontends intentionally remain separate for now: the runtime console is authoritative for the distributed simulation, while the Cesium app clearly executes a browser-local simulation.
- A pre-existing, concurrently authored modification to `scripts/download_model.sh` was preserved and excluded from this work.
