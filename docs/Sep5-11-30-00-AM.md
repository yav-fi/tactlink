# Model access setup

Completed: 2026-09-05 11:30:00 AM EDT

## Prompt

Bring over the Qwen 3.8 27B model from the user's other local model storage without
duplicating it on disk, so this project has access to it. Follow-up: set it up so each
team member downloads their own copy to run it, rather than committing weights.

## What changed

- Added `scripts/download_model.sh`, which pulls `mlx-community/Qwen3.8-27B-4bit` from
  Hugging Face into `models/qwen3.8-27b-4bit/` via `huggingface-cli`.
- Added `.gitignore` excluding `models/`, so weights are never committed.
- Documented setup and usage in `README.md`.
- Locally, symlinked the existing on-disk checkout at
  `~/dflash2-mlx/models/qwen3.8-27b-4bit-mlxcommunity` into `models/qwen3.8-27b-4bit` so
  this machine reuses it instead of redownloading; the symlink itself is gitignored and
  local-only.

## Verification

- Confirmed the symlinked model directory lists the expected weight/config files.
- Confirmed `git check-ignore` excludes `models/qwen3.8-27b-4bit` from tracking.
- Confirmed no other files were staged or modified beyond this change's scope.

## Notes

- This model is unrelated to the chat backend added in a later prompt, which uses a
  different DFlash2-accelerated serving path documented separately.
