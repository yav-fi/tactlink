# Merge main into a branch, add performance table

Completed: 2026-09-05 12:28:12 PM EDT

## Prompt

Don't push directly to `main` — always integrate on a separate branch first. Put the
pending requirements.txt commit on its own branch, pull latest `main`, and test-merge
without pushing. Also add a simple hardware/tok-s performance table to the README.

## What changed

- Created branch `requirements-cleanup` from the previously-unpushed
  `add requirements.txt, drop dead mlx-vlm dep` commit; reset local `main` to exactly
  match `origin/main` (which had diverged — a teammate merged in a separate "webcam
  gesture quadcopter control" workstream, `src/*.py` + a combined two-section README).
- Merged updated `main` into `requirements-cleanup` locally (not pushed, `main` itself
  untouched). One real conflict: `requirements.txt` was an add/add collision — the
  teammate's branch independently created the same filename with entirely different
  content (opencv-python/mediapipe/numpy for the drone workstream). Resolved by keeping
  both dependency sets in one file (LLM deps first, drone deps in a labeled second
  block) rather than picking one side. `README.md` and `.gitignore` auto-merged cleanly
  (git combined both sides' additions with no manual resolution needed).
- Added a `## Performance` table to the README's "Local LLM chat + benchmark" section:
  hardware (MacBook Pro, Apple M3 Max, 48GB — read via `system_profiler`, not guessed)
  and the real measured decode tok/s (72.3) from `benchmark_result.json`.

## Verification

- `git log origin/main..HEAD` on `main` is empty (confirms `main` wasn't advanced or
  altered by this work) — all new commits live only on `requirements-cleanup`, unpushed.
- Read `.gitignore` and `README.md` post-merge in full to confirm both sides' content
  survived intact (drone workstream sections, LLM workstream sections, both gitignore
  rule sets).
- Performance numbers pulled directly from the actual `benchmark_result.json` written by
  the earlier real gated benchmark run, not restated from memory.

## Notes, assumptions, limitations

- This branch has not been pushed or opened as a PR — that's a separate step, not done
  here, pending the user's go-ahead.
- The performance table currently has one row (this machine, this configuration) since
  that's the only real measurement taken so far.
