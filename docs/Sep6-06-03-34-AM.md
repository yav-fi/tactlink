# Technical pitch website

Completed: 2026-09-06 06:03:34 AM EDT (America/New_York, UTC-0400).

## Prompt

Build a professional hackathon pitch website in `pitch`, emphasizing GPS-denied / low-visibility camera-free relative positioning, explaining UWB, illustrating intersecting range spheres, round-robin scheduling around the single-partner radio constraint, timing statistics, and MediaPipe training/inference architecture. Avoid filler and ground diagrams in the system.

## Changes

- Added a responsive static-export React/Vinext site in `pitch/` with graphite/lime technical styling, local SVG diagrams, metadata, favicon, keyboard controls, and reduced-motion styling.
- Built interactive range constraints, five-round / ten-pair schedule with play/pause, a peer network diagram, the MDS geometry flow, separate radio/gesture lanes, and accessible training/inference tabs.
- Added engineering-envelope cards and source links to implementation and primary UWB/MediaPipe references.
- Added `pitch/README.md` with setup, diagram semantics, verification, and claim boundaries.
- Published private Sites version 1: https://tactlink-spatial-pitch.agentalvaru.chatgpt.site . Deployment status confirmed succeeded.

## Verification

- Final production build passed under Node 22.18.0; static root and 404 output emitted.
- TypeScript `tsc --noEmit` passed.
- Authored-code lint (`oxlint app lib vite.config.ts next.config.ts` from pitch) passed.
- Full starter-wide lint was attempted and fails in untouched generated UI components/hooks; retained those vendor files without changing or suppressing their rules.
- Local preview HTTP request returned 200 after resolving an unsupported icon import.
- Exported HTML check passed: one h1, all internal fragment links resolve, and local script/style assets exist.
- Diagram invariant checks passed: all ten unique pairs appear once, four distinct peers per round, and all five schematic range silhouettes meet the indicated point.
- Deployment archive validated and uploaded; generated dependencies/build/cache files excluded from Git. Source staged diff and credential-pattern scan reviewed.
- No browser screenshot, visual, or interaction QA was run, per Sites skill workflow for requests without explicit browser testing.

## Notes and boundaries

- Relative UWB positioning uses no camera. Optional MediaPipe gesture input does use one; frames stay local.
- The actual prototype elects a peer coordinator; it is infrastructure-free and peer-coordinated rather than leaderless. The site does not claim a fully distributed solver.
- The requested <5-second complete-cycle statistic is explicitly a design target: no real-device cycle benchmarks were available. Low-visibility field performance is likewise not presented as measured.
- 100 ms coordinator tick and roughly 10 Hz telemetry are distinguished from fresh ranging cycles. Four readings are a completion milestone, not proven accuracy.
- The five-shell visual explains point constraints; actual five-phone geometry uses ten mutual distances and multidimensional scaling.
- No simulator, flight, iOS, or existing user-authored files changed. The pre-existing staged `docs/Sep6-03-32-00-AM.md` is excluded from this commit and preserved.
- Sites source publishing used a temporary independent source copy, leaving `pitch` a regular folder in this monorepo.
