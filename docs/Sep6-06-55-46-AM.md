# Optional PC battery mode

Completed: 2026-09-06T06:55:46.263840-04:00 (America/New_York).

Request: implement optional battery simulation on the battery branch, preserve existing UI/functionality, exclude phone control, and push.

Added an off-by-default collapsible PC-only panel, per-drone battery profiles and custom capacity/mass/health, movement-based energy consumption, reserve/direct-return estimates, depleted-flight blocking, explicit replacement, and battery snapshot/undo support. Existing phone and backend modes never enable it.

Verification: all 75 mapping tests passed, including five new energy/controller regressions. Mapping production build passed; browser checks confirmed initial off state, enabled power/charge updates while gestures move the drone, rendering, and no panel in phone mode. git diff --check passed. An initial root build invoked the unrelated pitch project (missing vinext); validation was corrected to the mapping package.

Limitations: configurable uncalibrated energy model, advisory straight-line return estimate, no full mission energy gate or automated landing/return. No phone code changed.
