# ArduPilot and teammate visualizer bridge

Completed: September 5, 2026, 01:49:00 PM EDT (America/New_York, UTC-04:00).

Prompt: Build an ArduPilot connection and a connection to the teammate's
visualizer; user then requested checking the teammate's newly pushed simulator.

Fetched origin and fast-forwarded to the merged Cesium simulator (PR #3,
68096bd). Inspected its actual mission, coordinate, and controller contracts.
Added validated ENU path segments, sampled orbits, Mission Planner WPL export,
an optional pymavlink telemetry reader, and a standalone FastAPI HTTP/WebSocket
bridge on port 8765. Added a text-preview control and adapter in the existing
Cesium app, a route overlay, and conversion into its native goto/hover mission
steps. No arming, flight-mode changes, mission upload, or execution is sent over
MAVLink. Documented local setup, LAN settings, message schemas, and limitations.

Validation: 16 focused Python tests passed, including real localhost MAVLink
UDP packets, source filtering, staleness, HTTP/WebSocket delivery, path geometry,
export fields, and existing language tests. All eight simulator Node tests
passed, including running the converted path through the teammate's actual
DroneController. TypeScript checking and Vite production build passed. Build
and dependency setup required sandbox escalation. A FastAPI test-client
deprecation notice is non-failing. No physical vehicle or ArduPilot SITL instance
was available; no live autopilot flight or visual browser inspection is claimed.

The renderer's home height is ellipsoidal; its preview is relative to deployment,
while exported aircraft missions require an explicit MSL home origin. Landing
in the renderer returns to deployment height. The renderer's native orbit has
different center semantics, so the bridge preserves our orbit using sampled
goto points. WPL exports likewise approximate arcs as waypoint polygons.
Telemetry is exposed for downstream use; the Cesium UI does not yet show a live
aircraft marker. Speech input remains deferred. Existing/concurrent teammate
changes are excluded from this commit.
