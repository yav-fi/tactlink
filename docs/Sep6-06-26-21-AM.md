# Fix Vercel pitch deployment

Completed configuration: 2026-09-06 06:26:21 AM EDT (America/New_York, UTC-0400).

## Prompt

Fix the Vercel deployment failure detecting `src/main.py` as a FastAPI application, for the pitch site at `dnhacks26.arulandu.com`.

## Changes

- Added root `vercel.json` selecting Other (`framework: null`), `npm --prefix pitch ci`, `npm --prefix pitch run build`, and `pitch/dist/client` as the public output. This overrides the incorrectly detected FastAPI preset without changing Python entrypoints.
- Added a small root Node package manifest with Node 22.x and pitch build/dev scripts; existing dependency lockfile stays in pitch.
- Set the canonical URL and metadata base to `https://dnhacks26.arulandu.com`.
- Documented Vercel configuration and the requirement to build the new commit rather than redeploy the old failing SHA.
- Mirrored these changes in the original working directory while preserving all concurrently staged phone/gesture changes. Destination uses the separate `arulandu/dn` checkout and independent history established in the prior prompt.

## Verification

- Read Vercel's failed deployment and settings using browser control: saved preset FastAPI, repository root, Node 24 default. Repository config explicitly overrides the framework and Node version.
- Confirmed the custom domain is already assigned to Production; Vercel shows No Deployment before this fix ships.
- Exact clean install command passed using the committed pitch lockfile under Node 22.18.0.
- Exact production build command passed; root and 404 pages statically exported.
- TypeScript `tsc --noEmit` passed.
- Export checks passed for canonical URL and referenced JS/CSS assets.
- Initial sandboxed build was denied the local socket needed for prerender; reran successfully with permitted local socket access.
- No application secrets, Python runtime, or server functions are required for this pitch deployment. Existing starter dependency audit reports were left unchanged; no unrelated dependency upgrades were made.
- Vercel production deployment `6MC8BpKgmQGEXPWDEyp33hbCyZnc` for app fix `9980bd5` reached Ready in 32 seconds and was assigned to `dnhacks26.arulandu.com`.
- Live HTTPS check returned HTTP 200 with TactLink content; all four referenced JavaScript/CSS assets loaded successfully. System curl certificate verification remained enabled (the initial Python probe had a local trust-store error).
- The original workspace received a local-only mirror commit. Only `arulandu/dn` was pushed, preserving the previously established disclosure boundary for the original upstream.

Reference: https://vercel.com/docs/project-configuration/vercel-json (framework, installCommand, buildCommand, outputDirectory overrides).
