# TactLink branding update

**Completed:** September 5, 2026 at 11:23:57 PM EDT (UTC-04:00)

## Prompt

Rename the project and its primary titles to TactLink, and add a more patriotic
mission statement focused on helping teams use finger gestures to direct drones
during reconnaissance when conventional piloting takes too much attention.

## What changed

- Renamed the root README title to **TactLink** and added the mission and product
  premise at the beginning.
- Updated the browser page title, metadata, eyebrow, and main heading.
- Branded the 3D mission-control, iOS positioning, and planning README headings.
- Changed the iOS home-screen display name and relevant permission descriptions
  to TactLink while preserving internal project, scheme, bundle, and protocol
  identifiers for compatibility.

## Verification

- `npm run build` in `3d-mapping/` — passed (`tsc --noEmit` and Vite production build).
- `plutil -lint ios/SignalMap/Info.plist` — passed.
- `git diff --check` — passed.

## Notes

- Existing internal names such as `SignalMap.xcodeproj`, source directories, and
  the `_signal-room._tcp` Bonjour service were intentionally not renamed.
- The repository's pre-existing README update describing private, in-browser
  gesture recognition was preserved.
